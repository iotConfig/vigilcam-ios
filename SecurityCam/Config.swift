// Config.swift
//
// All user-configurable values (SMTP credentials, device name, etc.) are now
// stored in UserDefaults via SettingsModel and edited through SettingsView.
// There are no hardcoded secrets in this file.
